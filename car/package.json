{
  "name": "cassette-car",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper that runs the Cassette PWA as an Android app with real media-session controls (steering wheel / Bluetooth / Android Auto notification).",
  "scripts": {
    "sync": "cap sync android",
    "apk:debug": "cap sync android && cd android && gradlew.bat assembleDebug",
    "apk:release": "cap sync android && cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@jofr/capacitor-media-session": "^4.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
